import gulp from 'gulp';
import mocha from '<%- gulpMocha %>';

import '<%- importPreTestTask %>';

const testGlob = <%- testGlob %>;

export const test = () => {
  return gulp.src(testGlob)
    .pipe(mocha());
};

gulp.task('test', gulp.series('<%- preTestTask %>', test));
