import { expect } from "chai";
import <%- className %> from "<%- relPath %>/<%- srcDir %>/<%- file %>";

describe("Testing <%- className %>", (): void => {
  const defaultArgs = [];

  it("Class <%- className %> can be instanciated", (): void => {
    expect(
      (): void => {
        new <%- className %>(...defaultArgs);
      }
    ).not.to.throw();
  });
});
