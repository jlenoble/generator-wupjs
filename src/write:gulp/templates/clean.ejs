import {task} from "gulp";
import del from "del";

export const clean = () => {
  return Promise.all([
    del("<%- buildDir %>")<% if (antlr4 && parserDir) { %>,
    del("<%- parserDir %>")<% } %>
  ]);
};

task("clean", clean);
