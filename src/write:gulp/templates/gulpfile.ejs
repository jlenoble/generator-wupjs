import gulp from "gulp";
import {usePlumbedGulpSrc} from "plumb-gulp";
import autoreload from "autoreload-gulp";
<% for (let i = 0, l = gulpIncludes.length; i < l; i++) { %>
import "<%- "./" + gulpDir + "/" + gulpIncludes[i] %>";<% } %>

usePlumbedGulpSrc();

gulp.task("default", autoreload("tdd"));
